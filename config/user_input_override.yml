# user_input.yml corresponds to the default parameters that can be found in the stage SQL queries 
# Those are always played if not overwritten by user_input_override.yml in the same folder
user_input:
  salesforce_host:
  salesforce_user: 
  salesforce_password:
  salesforce_database:
  oracle_host:
  oracle_user:
  oracle_password: 
  oracle_database: